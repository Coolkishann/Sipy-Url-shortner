server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static assets from the /assets directory (Vite default build output)
    location /assets/ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, no-transform";
    }

    # Serve files from /styles and /scripts if they exist (Legacy or dev fallback)
    location /styles/ {
        try_files $uri =404;
    }
    
    location /scripts/ {
        try_files $uri =404;
    }

    location /favicon.ico {
        try_files $uri =404;
    }

    # API & Health Checks
    # For local docker-compose use http://load-balancer:8080
    location /api/ {
        proxy_pass https://sipy-url-shortner-1.onrender.com;
        proxy_set_header Host sipy-url-shortner-1.onrender.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /health {
        proxy_pass https://sipy-url-shortner-1.onrender.com;
        proxy_set_header Host sipy-url-shortner-1.onrender.com;
    }

    # Default location for the frontend
    location / {
        # Try to serve requested files first, then fallback to API (for short links)
        # If it's a short link (like /abc123), it will fall through to @backend
        try_files $uri $uri/ @backend;
    }

    # Short Link Redirect - Anything not found as a static file goes to the API
    location @backend {
        proxy_pass https://sipy-url-shortner-1.onrender.com;
        proxy_set_header Host sipy-url-shortner-1.onrender.com;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;

        # Handle 404s by falling back to index.html (useful for SPA behavior)
        proxy_intercept_errors on;
        error_page 404 = /index.html;
    }
}